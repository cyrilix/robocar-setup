ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1
country=FR

network={
        ssid=\"${WIFI_SSID}\"
        psk=\"${WIFI_PASSWORD}\"
        priority=2
}

network={
        ssid=\"${WIFI2_SSID}\"
        psk=\"${WIFI2_PASSWORD}\"
        priority=2
}
